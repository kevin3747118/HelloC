cmake_minimum_required(VERSION 3.29.0) # Specify the minimum version of CMake required

project(MyProject VERSION 1.0) # Define the project name and version

add_executable(MyExecutable hello.c) # Define an executable target named MyExecutable from main.cpp

